mixin mainCardTemplate(title,className)
	div(class="theme Mtop10 " + className)
		.title.bgtitlecolor.title-color
			h4.square= title
		.borderAll.around5.padd5
			block
mixin formTemplate(title,className)
	div(class="formtheme Mtop10 " + className)
		.formtitle
			span= title
		.formbody
			block
